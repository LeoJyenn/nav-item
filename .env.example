# 服务端口（默认 3000）
PORT=3216

# 管理员初始账号（仅首次自动建库时生效）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=123456

# JWT 密钥（可选，不设置更安全省心）
# 留空时首次启动自动生成 96 位强随机密钥并保存到 data/jwt-secret.key，
# 之后重启复用；仓库与镜像中不含任何可用密钥。
# 如需跨数据目录固定密钥（如多实例部署）再手动设置：
# JWT_SECRET=
